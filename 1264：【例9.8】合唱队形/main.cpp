#include "bits/stdc++.h"
using namespace std;
using vi = vector<int>;



int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    ifstream in("in");
    cin.rdbuf(in.rdbuf());

    int N;
    cin >> N;
    vi T(N);
    for(auto &t : T) {
        cin >> t;
    }

    return 0;
}