#include <bits/stdc++.h>
#define ci cin>>
#define co cout<<
using namespace std;

int main()
{
    freopen("in", "r", stdin);
    int T, id;
    ci T >> id;
    while (T--)
    {
        int n, m, c, f;
        ci n >> m >> c >> f;
        vector<string> str(m,string(n,' '));
        for(string& s:str) ci s;

    }
}