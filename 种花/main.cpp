#include <bits/stdc++.h>
using namespace std;

int main()
{
    freopen("in", "r", stdin);
    int T, id;
    cin >> T >> id;
    for (; T--; T > 0)
    {
        int n, m, c, f;
        cin >> n >> m >> c >> f;
        vector<string> str(n,string(m,' '));

    }
}